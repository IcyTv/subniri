[package]
name = "icons"
version = "0.1.0"
edition.workspace = true

[dependencies]
gtk4.workspace = true
workspace-hack = { version = "0.1", path = "../workspace-hack" }

[build-dependencies]
glib-build-tools.workspace = true
heck.workspace = true
